export type TLocation = {
    place_id?: number,
    licence?: string,
    boundingbox?: string[],
    lat?: number,
    lon?: number,
    display_name?: string,
    class?: string,
    type?: string,
    importance?: number,
    distance?: string | number,
}
